# Jasper Music Bot - Agent Instructions

> **META-INSTRUCTION**: You should read `AGENTS.md` on the start of each run on this project to understand the project better and update `AGENTS.md` with any critical details as needed.


## 1. Project Overview
**Jasper** is a Discord music bot themed after a big black Persian cat. It is built with **Node.js** and **discord.js v14**.
Its core feature is high-quality audio streaming using **yt-dlp** (an external command-line tool) to bypass YouTube's restrictions, making it more resilient than bots using standard libraries like `ytdl-core`.

### Key Technologies
- **Runtime**: Node.js (v18+)
- **Language**: TypeScript (strict mode)
- **Module System**: ES Modules (`import`/`export`)
- **Framework**: discord.js v14
- **Voice Library**: @discordjs/voice
- **Audio Source**: `yt-dlp` (spawned as a child process)
- **Search**: `yt-search`
- **Architecture**: Multi-bot Worker Pool (supports multiple simultaneous voice channels)
- **Dev Tooling**: tsx (TypeScript execution with hot-reloading)

## 2. Setup & Environment

### Prerequisites
- Node.js v18+
- FFmpeg (static binary usually handled by `ffmpeg-static` or system installed)
- **yt-dlp**: This is CRITICAL. The bot searches for a `yt-dlp` (or `yt-dlp.exe`) binary using a custom helper (`src/utils/yt-dlp-helper.ts`), first checking the system's PATH, and then falling back to the project's **ROOT** directory.
- **Additional Worker Bots** (Optional): You can add additional worker bots by adding environment variables in the format `<NAME>_TOKEN=...` (e.g., `MISTY_TOKEN=...`). These will be automatically loaded and used to handle multiple voice channels simultaneously.

### Installation
1. `npm install`
   - **Note**: The `postinstall` script (`scripts/install-yt-dlp.js`) attempts to automatically download the correct `yt-dlp` binary for the OS.
   - If this fails, you must manually download `yt-dlp` and place it in the project root.
2. Create `.env` from `.env.example`:
   ```env
   DISCORD_TOKEN=...
   CLIENT_ID=...
   GUILD_ID=... (for dev command deployment)
   ```

### Running the Bot
- **Dev**: `npm run dev` (runs TypeScript with tsx watch)
- **Build**: `npm run build` (compiles TypeScript to JavaScript in `dist/`)
- **Start**: `npm start` (runs compiled JavaScript from `dist/`)
- **Deploy Commands**: `npm run deploy:commands` (Run this after creating or modifying command definitions)
- **Lint**: `npm run lint` (runs ESLint with TypeScript rules)

## 3. Architecture

### Directory Structure
```
Jasper/
├── src/
│   ├── commands/       # Slash command definitions (e.g., play.ts, stop.ts)
│   ├── config/         # Configuration (bots.ts - Multi-bot setup, config.ts)
│   ├── core/           # Core logic
│   │   ├── audio/      # Audio modules (queue-manager.ts, playback-engine.ts, stream-handler.ts)
│   │   ├── ui/         # UI modules (player-controls.ts)
│   │   ├── utils/      # Core utilities (voice-utils.ts)
│   │   ├── music-player.ts # Facade for audio logic
│   │   ├── worker-pool.ts  # Worker bot management
│   │   └── logger.ts       # Logging utility
│   ├── events/         # Event handlers (ready.ts, interaction-create.ts)
│   ├── types/          # TypeScript type definitions (discord.d.ts, command.ts)
│   ├── utils/          # General utilities (yt-dlp-helper.ts, embed-factory.ts)
│   ├── deploy-commands.ts # Script to register slash commands
│   └── index.ts        # Entry point
├── dist/               # Compiled JavaScript (generated by `npm run build`)
├── scripts/            # Maintenance scripts (install-yt-dlp.js)
├── .env                # Environment variables
├── tsconfig.json       # TypeScript configuration
├── eslint.config.mjs   # ESLint configuration
├── package.json
└── yt-dlp            # (Binary) Required for playback
```

### Core Components

#### 1. Entry Point (`src/index.ts`)
- Initializes the `discord.js` Client with typed commands collection.
- Dynamically loads commands from `src/commands` using `import()`.
- Dynamically loads events from `src/events` using `import()`.
- Logs in using `DISCORD_TOKEN`.
- **Type Safety**: Uses module augmentation to extend Discord.js Client with typed `commands` property.

#### 2. Configuration (`src/config/bots.ts`)
Defines the bot configuration for the Worker Pool:
- **Controller Bot**: Jasper (the main bot with slash commands) is defined with `DISCORD_TOKEN`.
- **Worker Bots**: Dynamically loaded from environment variables ending in `_TOKEN` (e.g., `MISTY_TOKEN`, `SHADOW_TOKEN`).
  - Each worker is automatically named based on the env var name.
  - Workers handle playback duties, allowing multiple simultaneous voice channels.
- **Type Safety**: Uses `BotConfig` interface for type-safe bot configuration.

#### 3. Worker Pool (`src/core/worker-pool.ts`)
Manages multiple bot instances to enable concurrent playback:
- **Bot Creation**: Creates discord.js Client instances for all configured bots.
- **Login Management**: Logs in the controller first, then all workers in parallel.
- **Worker Allocation**: Assigns workers to voice channels using a priority system:
  1. Reuse worker already in the target channel.
  2. Prioritize idle Controller (Jasper).
  3. Use any idle worker bot.
  4. Return null if all workers are busy.
- **State Tracking**: Maintains each worker's busy status, assigned guild, and voice channel.
- **Cleanup**: Releases workers when playback ends.
- **Type Safety**: Uses `WorkerState` interface for type-safe worker management.

#### 4. Music Player (`src/core/music-player.ts`)
This is the facade for the audio system. It orchestrates the following modules:
- **Queue Manager** (`src/core/audio/queue-manager.ts`): Manages `Map<VoiceChannelId, QueueObject>` state with typed `Queue` and `Song` interfaces.
- **Stream Handler** (`src/core/audio/stream-handler.ts`): Handles `yt-dlp` process spawning and metadata fetching with typed return values.
- **Playback Engine** (`src/core/audio/playback-engine.ts`): Handles the `AudioPlayer`, event listeners, and "Now Playing" UI updates.
- **Player Controls** (`src/core/ui/player-controls.ts`): Generates button components with proper TypeScript types.
- **Voice Utils** (`src/core/utils/voice-utils.ts`): Helper functions for voice status and validation with type guards.

#### 5. Commands (`src/commands/*.ts`)
- Each file exports a default object implementing the `Command` interface:
  - `data`: A `SlashCommandBuilder` instance.
  - `execute(interaction)`: The function called when the command is used.
  - `autocomplete(interaction)`: (Optional) Handles autocomplete requests (used in `play.ts`).
- **Type Safety**: All commands use the `Command` interface for full type safety and autocomplete.
- **Notable Commands**:
  - `play.ts`: Supports both keyword search (with autocomplete) and direct YouTube URLs. **Note**: Rejects playlist URLs (use `/playlist` instead).
  - `playlist.ts`: Supports adding YouTube playlists. Automatically truncates autogenerated playlists (e.g., "Mix - ...") to 50 songs for performance.
  - `catastrophic-reset.ts`: Emergency command to clear all queues and reset all workers.
  - `music-status.ts`: Displays the status of all bots in the cluster.

#### 6. Event Handlers (`src/events/*.ts`)
- `ready.ts`: Runs once when the bot connects. Sets bot presence.
- `interaction-create.ts`: Routes interactions to the appropriate command or autocomplete handler with proper type checking.

#### 7. Type Definitions (`src/types/*.ts`)
- `discord.d.ts`: Module augmentation to extend Discord.js `Client` with typed `commands` property.
- `command.ts`: `Command` interface for type-safe slash command definitions.

#### 8. Utilities (`src/utils/*.ts`)
- `yt-dlp-helper.ts`: Encapsulates the logic for finding the `yt-dlp` binary on the system or in the project root.
- `embed-factory.ts`: Helper functions for creating consistent Discord embeds.

## 4. Development Workflows

### Adding a New Command
1. Create a new file in `src/commands/` (e.g., `mycommand.ts`).
2. Boilerplate (TypeScript):
   ```typescript
   import { SlashCommandBuilder, ChatInputCommandInteraction } from "discord.js";
   import { Command } from "../types/command.js";

   export default {
     data: new SlashCommandBuilder()
       .setName("mycommand")
       .setDescription("Does something cool"),
     async execute(interaction: ChatInputCommandInteraction) {
       await interaction.reply("Hello!");
     },
   } satisfies Command;
   ```
3. Run `npm run deploy:commands` to register it with Discord.
4. Restart the bot.

### Modifying Audio Logic
- Edit `src/core/music-player.ts` or its sub-modules in `src/core/audio/`.
- **Important**: `src/core/audio/stream-handler.ts` handles the `yt-dlp` process spawning. Use `src/utils/yt-dlp-helper.ts` to resolve the binary path.
- **Worker Pool**: Music player requests workers via `workerPool.allocateWorker()` before creating a queue. Always release workers with `workerPool.releaseWorker()` when done.
- **Queue Management**: The `queues` Map is keyed by `voiceChannelId` (not `guildId`) to support multiple channels per guild. Ensure you handle concurrency correctly.
- **Type Safety**: All functions have explicit type annotations. Use the defined interfaces (`Queue`, `Song`, `WorkerState`) for type safety.

### Handling Interactions
- If you add new UI components (buttons, select menus), handle them in the `interaction-create.ts` event or within the command that spawned them (using a `ComponentCollector`).
- `playback-engine.ts` currently uses a `ComponentCollector` on the "Now Playing" message to handle playback buttons.

## 5. Common Issues & Debugging

### "yt-dlp not found"
- **Cause**: The binary is missing from the root directory and system PATH.
- **Fix**: Run `npm run postinstall` or manually download `yt-dlp` and place it in the root.

### Audio Stops / 403 Errors
- **Cause**: YouTube has updated their anti-bot measures.
- **Fix**: Update the `yt-dlp` binary.
  - Stop the bot.
  - Replace the `yt-dlp` binary with the latest release.
  - Restart.

### Permissions
- The bot needs `Connect` and `Speak` permissions in voice channels.
- It needs `Send Messages` and `Embed Links` in text channels.
- It needs `Manage Channels` (or specific Voice Channel permissions) to update Voice Channel Status.
- **Worker Bots**: Each worker bot also needs `Connect` and `Speak` permissions. Ensure all worker bots are invited to your server.

## 6. Code Style
- **Language**: TypeScript with strict mode enabled.
- **Format**: ES Modules with `.js` extensions in imports (required for ESM).
- **Type Safety**: All functions have explicit type annotations. Avoid `any` types.
- **Imports**: Use `import` statements. File extensions (`.js`) are mandatory for local imports even when importing `.ts` files.
- **Async/Await**: Used extensively for Discord API calls and file I/O.
- **Logging**: Use `src/core/logger.ts` instead of raw `console.log` where possible.
- **Error Handling**: Use typed error handling with `unknown` and proper type narrowing.

## 7. TypeScript Development
- **Build**: Run `npm run build` to compile TypeScript to JavaScript in `dist/`.
- **Dev Mode**: Use `npm run dev` for hot-reloading with `tsx`.
- **Linting**: Run `npm run lint` to check for TypeScript and ESLint errors.
- **Type Checking**: TypeScript compiler will catch type errors during build.
- **Interfaces**: Use defined interfaces (`Command`, `Queue`, `Song`, `WorkerState`, `BotConfig`) for type safety.

---
*Generated by Antigravity*
