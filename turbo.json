{
    "$schema": "https://turbo.build/schema.json",
    "tasks": {
        "build": {
            "dependsOn": ["^build"],
            "outputs": ["dist/**", ".next/**"]
        },
        "lint": {},
        "typecheck": {},
        "test": {
            "dependsOn": ["^build"],
            "env": ["DB_TYPE", "DATABASE_URL"]
        },
        "dev": {
            "cache": false,
            "persistent": true
        }
    }
}
